# Сервис выдачи промокодов

Документ требований
1. Введение и область применения
Введение

Настоящий документ описывает требования к проектированию и разработке микросервиса выдачи промокодов, предназначенного для поддержки программ лояльности клиентов и автоматизации маркетинговых акций.

Область применения

Сервис отвечает за:

хранение промокодов;
выдачу промокодов пользователям;
проверку права пользователя на получение промокода;
контроль повторной выдачи промокодов в рамках одной акции.

Сервис не отвечает за:

создание и настройку маркетинговых акций;
регистрацию пользователей;
применение промокодов при оформлении заказа.
2. Цели и задачи
Цели
предотвращение повторной выдачи промокода одному пользователю в рамках одной акции;
автоматизация проверки права пользователя на получение промокода;
снижение нагрузки на основную платформу в период проведения маркетинговых акций.
Задачи
спроектировать микросервис со своей собственной базой данных;
разработать структуру реляционной базы данных для хранения пользователей, акций, промокодов и истории выдач;
реализовать автоматическую проверку права пользователя на получение промокода;
обеспечить сохранение информации о каждой успешной выдаче промокода.
3. Требования
3.1 Функциональные требования
Система должна автоматически проверять статус акции при каждом запросе на получение промокода.
Система должна определять, выдавался ли ранее промокод пользователю в рамках данной акции.
Система должна блокировать повторную выдачу промокода одному пользователю в рамках одной акции.
При успешной проверке система должна выбрать свободный промокод из базы данных.
После успешной выдачи система должна сохранить информацию о выдаче в таблице issuances, включая:
идентификатор пользователя (user_id);
идентификатор акции (action_id);
идентификатор промокода (promocode_id);
дату и время выдачи.
После успешного сохранения записи система должна вернуть пользователю промокод.
3.2 Нефункциональные требования
Время проверки права пользователя на получение промокода не должно превышать 50 мс.
Время обработки запроса на выдачу промокода не должно превышать 200 мс.
Не менее 95% успешных запросов должны выполняться за время не более 200 мс.
Сервис должен обеспечивать доступность не ниже 99% времени в течение месяца.
Система должна корректно обрабатывать одновременные запросы на получение промокода одним пользователем без повторной выдачи.
4. Критерии приемки
Сценарий	Результат
Пользователь получает промокод впервые	200 OK
Пользователь уже получал промокод	409 Conflict
Срок действия акции истек	422 Unprocessable Entity
Свободные промокоды отсутствуют	409 Conflict
5. Требования к тестированию
Функциональное тестирование

Проверить:

успешную выдачу промокода;
повторную выдачу одному пользователю;
окончание срока действия акции;
отсутствие свободных промокодов.
Нагрузочное тестирование

Сервис должен выдерживать нагрузку 500 запросов в секунду.

Тестирование конкурентных запросов

При одновременной отправке нескольких запросов одним пользователем сервис должен выдать только один промокод.

6. Приложения

Приложение 1 — Use Case
Приложение 2 — BPMN
Приложение 3 — UML Sequence Diagram
Приложение 4 — REST API (OpenAPI)
Приложение 5 — ERD (модель базы данных)

Приложение 4 — REST API (OpenAPI)

Приложение 5 — ERD (модель базы данных)
